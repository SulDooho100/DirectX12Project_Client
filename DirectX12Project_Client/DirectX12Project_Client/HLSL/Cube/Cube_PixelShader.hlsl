#include "../Header/VertexTypes.hlsli"
#include "../Header/ConstantBuffers.hlsli"

float4 main(VectexPositionNormalTangentBinormalTexture_Output input) : SV_TARGET
{ 
    return float4(1, 0, 0, 1);

}